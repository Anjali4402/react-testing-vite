
const SnapshotTesting = () => {
  return (
    <div>
        <h1>Snapshot Testing</h1>
        <p>Snapshot testing is like taking a picture of your UI or component output and saving it. Later, when you run your tests again, it compares the current output to the saved picture (snapshot) to check if anything has changed.</p>
    </div>
  )
}

export default SnapshotTesting